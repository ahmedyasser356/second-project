on:
  push:
    branches:
      - master




jobs:
  build:
   runs-on: ubuntu-latest

   steps:
     - name: "print name "
       run: echo Ahmed 
     uses: actions/checkout@v3
     - name: "all elements"
       run: ls 
